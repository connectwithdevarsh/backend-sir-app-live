import 'package:flutter/material.dart';
import 'faculty_profile_screen.dart';

/// SirProfileScreen serves as the entry point for the Sir Profile tab.
class SirProfileScreen extends StatelessWidget {
  const SirProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const FacultyProfileScreen();
  }
}
